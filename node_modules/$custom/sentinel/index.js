const config = require('$custom/config');
const socket = require('socket.io-client')('http://localhost:811');

module.exports = {
	client: function(){
		return socket;
	},
	api: function(){
		socket.emit('no-client', {});
		return socket;
	},
	events: {
		
	}
}